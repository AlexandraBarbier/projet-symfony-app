{% extends 'base.html.twig' %}

{% block title %}Edit Plat{% endblock %}

{% block body %}
    <h1>Edit Plat</h1>

    {{ include('admin/plats/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('admin_plats_index') }}">back to list</a>

    {{ include('admin/plats/_delete_form.html.twig') }}
{% endblock %}
